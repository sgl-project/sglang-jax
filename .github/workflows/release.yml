name: Release (Publish Artifacts)

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing tag to re-publish (e.g. v0.0.3rc0). Use any plausible tag name in dry-run mode."
        required: true
        type: string
      dry_run:
        description: "If true, build artifacts but skip PyPI/Docker publish and GitHub Release creation"
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  metadata:
    if: github.repository == 'sgl-project/sglang-jax'
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.compute.outputs.tag }}
      version: ${{ steps.compute.outputs.version }}
      is_prerelease: ${{ steps.compute.outputs.is_prerelease }}
      update_latest: ${{ steps.compute.outputs.update_latest }}
      dry_run: ${{ steps.compute.outputs.dry_run }}
    steps:
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install packaging
        run: python -m pip install --upgrade packaging

      - name: Compute tag / version / flags
        id: compute
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            TAG="${{ inputs.tag }}"
            DRY_RUN="${{ inputs.dry_run }}"
          else
            TAG="${GITHUB_REF_NAME}"
            DRY_RUN="false"
          fi
          if [[ ! "${TAG}" =~ ^v.+ ]]; then
            echo "::error::Tag '${TAG}' does not start with 'v'"
            exit 1
          fi
          VERSION="${TAG#v}"
          # PEP 440 validation + prerelease detection.
          # Covers aN / bN / rcN / .devN as prereleases; .postN is a post-release (not pre).
          IS_PRERELEASE="$(VERSION="${VERSION}" python -c '
          import os, sys
          from packaging.version import Version, InvalidVersion
          v_str = os.environ["VERSION"]
          try:
              v = Version(v_str)
          except InvalidVersion as e:
              sys.stderr.write(f"::error::Invalid PEP 440 version {v_str!r}: {e}\n")
              sys.exit(1)
          print("true" if v.is_prerelease else "false")
          ')"
          if [[ "${IS_PRERELEASE}" == "true" ]]; then
            UPDATE_LATEST=false
          else
            UPDATE_LATEST=true
          fi
          {
            echo "tag=${TAG}"
            echo "version=${VERSION}"
            echo "is_prerelease=${IS_PRERELEASE}"
            echo "update_latest=${UPDATE_LATEST}"
            echo "dry_run=${DRY_RUN}"
          } >> "$GITHUB_OUTPUT"

  publish-pypi:
    needs: metadata
    uses: ./.github/workflows/release-pypi.yml
    secrets: inherit
    with:
      version: ${{ needs.metadata.outputs.version }}
      dry_run: ${{ needs.metadata.outputs.dry_run == 'true' }}

  publish-docker:
    needs: metadata
    uses: ./.github/workflows/release-docker.yml
    secrets: inherit
    with:
      version: ${{ needs.metadata.outputs.version }}
      update_latest: ${{ needs.metadata.outputs.update_latest == 'true' }}
      dry_run: ${{ needs.metadata.outputs.dry_run == 'true' }}

  github-release:
    needs: [metadata, publish-pypi, publish-docker]
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ needs.metadata.outputs.dry_run == 'true' && github.ref || needs.metadata.outputs.tag }}

      - name: Dry-run notice
        if: ${{ needs.metadata.outputs.dry_run == 'true' }}
        run: |
          echo "::notice::Dry-run mode: GitHub Release will NOT be created."
          echo "Would create release for ${{ needs.metadata.outputs.tag }} (prerelease=${{ needs.metadata.outputs.is_prerelease }})." >> "$GITHUB_STEP_SUMMARY"

      - name: Create GitHub Release
        if: ${{ needs.metadata.outputs.dry_run != 'true' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.metadata.outputs.tag }}
          name: sglang-jax ${{ needs.metadata.outputs.tag }}
          generate_release_notes: true
          prerelease: ${{ needs.metadata.outputs.is_prerelease == 'true' }}
          fail_on_unmatched_files: false
