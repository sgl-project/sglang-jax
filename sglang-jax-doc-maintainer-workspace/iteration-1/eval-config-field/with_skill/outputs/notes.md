# notes

- 按 skill 的 doc map，`server_args.py` 首先定位到 `13-configuration-reference.md`，`srt/mem_cache/**` 定位到 `07-kv-cache.md`。
- 选择“段落融入”而不是新章节，因为新增字段扩展已有内存配置和内存池初始化说明。
- 没有声称 hybrid pool 提升吞吐、降低显存或默认启用，因为 eval 输入只证明选择逻辑存在。
- 未模拟图表变更，因为 skill 要求图表单独确认。
