# notes

- 按 doc map，`srt/layers/attention/**` 主目标是 `06-layers-and-attention.md`，`07-kv-cache.md` 与 `08-pallas-kernels.md` 是语义确认后的次级候选。
- 没有跟随 baseline 修改 `04-model-executor.md`，因为输入没有 model executor 边界变化证据。
- 文档把通用 block sparse motivation 和项目代码事实分开，以避免把外部研究当作项目行为证明。
