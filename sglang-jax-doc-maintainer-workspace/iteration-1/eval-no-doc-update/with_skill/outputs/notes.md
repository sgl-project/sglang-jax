# notes

- 按 doc map，`.github/workflows/**` 与 `test/**` 默认不触发架构文档更新。
- 仍列出 `03-scheduler.md` 和 `development.md` 作为 no-update 候选，是为了显式说明为什么测试格式化和 CI cache key 不改变文档化架构。
- no-op 输出仍保留 impact report、update plan 和 validation statement，验证 skill 的 no-doc-update 行为。
